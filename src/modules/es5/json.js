define(function() {
  //            Firefox (Gecko) Chrome  Internet Explorer Opera Safari
  // In version 3.5             (Yes)   8.0               10.5  4.0

  if(!window.JSON) {
    // TODO: Include json3 for browsers that don't support JSON ??
  }
});
